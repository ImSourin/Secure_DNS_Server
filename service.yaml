apiVersion: v1
kind: Service
metadata:
  name: dns-service
  namespace: default
spec:
  selector:
    app: dns-server
  type: ClusterIP
  ports:
    - name: udp
      port: 31111
      targetPort: 31111
      protocol: UDP